class Continente {

    constructor(strokeColor, fillColor, provincias, bonus, code) {
        this.strokeColor = strokeColor;
        this.fillColor = fillColor;
        this.provincias = provincias;
        this.bonus = bonus;
        this.code = code;
    }
}
